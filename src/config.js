export const CONFIG = Object.freeze({
  // Deploy the Worker separately, then set its origin here or in the debug panel.
  // Example: "https://bobobo-creature-ai.example.workers.dev"
  workerUrl: "",
  aiCooldownMs: 60_000,
  aiTimeoutMs: 7_000,
});
